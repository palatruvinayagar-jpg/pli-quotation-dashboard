{
  "name": "pli-quotation-dashboard",
  "version": "0.1.0",
  "description": "PLI Quotation Dashboard — Postal Life Insurance (PLI) Endowment Assurance quotation tool",
  "main": "index.html",
  "scripts": {
    "start": "npx serve .",
    "serve": "npx serve .",
    "lint": "eslint \"src/**/*.js\" \"*.js\"",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "devDependencies": {
    "eslint": "^8.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
